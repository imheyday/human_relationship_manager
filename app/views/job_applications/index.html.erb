<div class="row wrapper border-bottom white-bg page-heading">
  <div class="col-lg-10">
    <h2><%= "#{t('home.menu.job_applications')} (#{@job_applications.count})" %></h2>
  </div>
</div>

<div class="wrapper wrapper-content animated fadeInRight ecommerce">
  <div class="row job_applicationsList">
    <div class="col-lg-12">
      <div class="ibox">
        <div class="ibox-title">
          <h5><%= t('job_application.index') %></h5>
          <div class="ibox-tools">
            <%= button_to new_job_application_path, class: 'btn btn-xs btn-primary', method: 'get' do %>
              <i class="fa fa-plus-square"></i>
              <%= t('job_application.messages.add') %>
            <% end %>
          </div>
        </div>
        <div class="ibox-content">
          <% if @job_applications.count > 0 %>
            <%= render 'job_applications_list', job_applications: @job_applications %>
          <% else %>
            <%= render 'no_results' %>
          <% end %>
        </div>
      </div>
    </div>
  </div>

</div>

<% content_for :javascript do %>

<script type="text/javascript">
  $(function() {
    $('.footable').footable();
  });
</script>

<% end %>
